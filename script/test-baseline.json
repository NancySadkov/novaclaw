{
  "note": "Baseline for `bun run test`. `units` = tests bun reported SKIPPED per run unit; `counts` = tests Bun reported completed per run unit; `failing` = failures PINNED as known, by name. All three are ratchets: any change fails the run and prints what moved. Pin only failures that are understood and filed — never to make a red go away. Counts seeded from the 2026-09-04 fast tier after the worker framing and dependency-ledger fixes. Counts re-synced 2026-09-12 after a full green run (23/23 units, 19/19 typechecks), then again the same day for FOUR added guards: `core` 7472→7475 is `session-runner-model.test.ts` (a pin to a switched-off model is void, the two fallback reasons are told apart, and `enabled` has one writer in the tree) and `app:unit` 2100→2101 is `editor-core.test.ts` (the caret is restored after a tab switch moved focus to a button). Both moved UP by exactly the tests added; nothing moved DOWN. Third re-sync the same day, after a full green run (23/23 units, 19/19 typechecks, 548.4s): `core` 7475→7480 and `app:unit` 2101→2104. `core` +5 is `nudge.test.ts` (2: the shipped time-safety nudge ignores what the agent READS and matches only what it writes; `text-match` still reads tool output so stored hooks are not silently narrowed), `nudge-service.test.ts` (1: no delivery to a session that is only reading or querying) and `session-compaction.test.ts` (2: a committing compaction publishes the window, reserve and threshold it measured against; overflow recovery is named as its own cause). `app:unit` +3 is `native-scroll.test.ts` (a shrinking viewport cannot leave a pinned view off the bottom; a DOM-owned move that leaves the view off the bottom cannot revoke the pin; the view is back at the bottom once the rows return). Every delta is a test ADDED and attributed; nothing moved DOWN. Earlier re-sync: SIX units moved, all UPWARD, and each delta is a test that was ADDED and never re-baselined — not a count that drifted on its own: `script` 242→249 is `script/lib/source-audit.test.ts` (7 cases, added 2026-09-10 by 5005df2d1), whose number had not been touched since it was written on 2026-09-05; `app:unit` 2089→2100 is `models-enablement.test.ts` (6) plus `model-override-source.test.ts` (5); `app:browser` 268→270 is `dom-failure-printing.test.ts` (2); `core` 7445→7472 is the model-switch silence, scheduler class-fault, recall, nudge, turn-timing, sampler and boot-recovery guards; `session-ui` 212→218 is `turn-receipt.test.ts`; `novaclaw:config` 127→130 is `nova-is-not-editable.test.ts`. No unit moved DOWN, so nothing was silently lost. FOURTH re-sync the same day, after a full green run (23/23 units, 19/19 typechecks, 536.0s): `app:unit` 2104→2105, which is `message-v2-fetch.test.ts` (+1 net). That guard is the scroll-to-top fix: `fetchNativeMessages` returned `response.data?.data ?? []`, so a payload-less response became an empty list, and `mergeNativeMessages` drops every local row for an EMPTY AUTHORITATIVE fetch because that is how a genuine full revert is expressed — the transcript blanked, the scroll container collapsed to the top, and the next fetch restored it. The function now returns `undefined` for an absent payload and the store skips its reconcile, so absence is no longer read as an ending; a genuine `[]` still reconciles and reverts keep working. The old test PINNED the defect (`expect(...).toEqual([])` for an absent payload) and was rewritten into two: one asserting `undefined`, one asserting `[]` is still `[]` — the second is what stops the fix from silently disabling reverts. Nothing moved DOWN. ⚠️ `peaks.core` is NOT touched by this re-sync: it read 20450 MB against the 10822 profile, which is the same stale-entry finding recorded on 2026-09-11 (every unsharded core run since Sep 11 reads ~22 GB, and sharded runs withhold the peak entirely). `peaksNote` says re-baseline the set or none of it, and `peaksLadderNote` rules explicitly against raising this entry on contended runs, so it stays. FIFTH re-sync the same day, after a full green run (23/23 units, 19/19 typechecks, 744.8s): `app:unit` 2105->2107, which is `reconnect-stream.test.ts` (+1: a never-settling `open()` is retried, not parked, once the attempt deadline aborts it) and `server-sdk.test.ts` (+1: the stream heartbeat is armed at attempt START, so a hanging open cannot park the loop). Both are the DEAD GLOBAL EVENT STREAM: `runReconnectingStream` awaits `open()` with NO timeout of its own and the 15s heartbeat was armed only INSIDE `open()`, after the SSE response resolves, so a half-open connection (TCP established, response headers never arriving) parked the loop forever -- no timer armed, no abort, no `failed()` log, no `state(\"reconnecting\")`, no reconnect banner and no retry. Measured 2026-09-12 in log/novaclaw.log: run=655af8cd went `disconnected` at 08:06:49.885Z and the next subscription did not arrive until 11:00:37.777Z, the same run id throughout, so the process was alive and the stream was dead for 2h53m48s. Same shape on 09-06 (3h13m), 09-07 (6h22m), 09-09 (4h05m) and 09-10 (3h27m), so it is chronic and not a one-off. Nothing moved DOWN. SIXTH re-sync the same day: `core` 7480→7493 (+13) covers DeepSeek reasoning replay, explicit non-retryable halt, seven-minute truthful worker joins (tokens, partial output, provider failures and completion races), and the fixed host-side timeout; `llm` 496→498 (+2) covers required-versus-ordinary `reasoning_content`; `app:unit` 2107→2109 (+2) covers compaction-event derivation. Every count moved UP by the tests added; nothing moved DOWN. SEVENTH re-sync the same day: `app:unit` 2109→2108 is the intentional net replacement of the two deleted memory-count formatting tests with one Back-route contract; `app:browser` 270→272 adds the empty-roster memory-cabinet regression and the addressable Contacts configure-return regression. No behavior guard was silently lost. EIGHTH re-sync the same day: `app:browser` 273→277 adds two officer-settings layout and portrait-state guards plus two tuning provenance guards; nothing moved down. NINTH re-sync the same day: `session-ui` 218→222 is `shell-card.test.ts` (+1: elapsed command and worker waits show the executor's timeout) and `tool-file-path.test.ts` (+3: relative host paths, absolute path forms, and every built-in file mutation); nothing moved down. TENTH re-sync the same day: `app:browser` 277→280 is `native-transcript-remount.test.tsx` (+3: the run-wide token count continues through tool input, command and worker waits show their actual deadlines, and an expanded file mutation shows its absolute host path); nothing moved down. ELEVENTH re-sync on 2026-09-13: `app:browser` 280→281 is `tab-close-invariant.test.tsx` (+1: Home is inert when already open even if a stale recent tab remains stored); nothing moved down.",
  "countsNote": "2026-09-13 officer-settings restoration re-sync: core 7522→7525 adds the durable operation-mode materialisation, stagnant unattended sleep, and five officer work-default guards; app:unit 2003→2005 adds two personality JSON boundary guards; app:browser 244→245 proves the restored Mind, Work, Nudges, Input / Output, and import/export controls live in their intended tabs. Nothing moved down. Earlier the same day, tmp-source-boundary re-sync: script 249→250 adds the forced-tracked tmp rejection; no tmp path may enter a source archive even if someone bypasses `.gitignore`. Earlier the same day, live model-switch re-sync: core 7518→7522 adds two stale-worker routing/dispatch behaviors, one no-contact utility behavior, and one source ratchet covering ordinary, Strict, short-answer, maintenance and compaction dispatch; app:unit 2001→2003 adds two quiet-model-overview guards. Nothing moved down. Earlier the same day, memory-atlas re-sync: app:unit 2118→2001 is the intentional retirement of 123 tests coupled to the scrapped legacy memory graph, replaced by five deterministic WebGL-atlas model tests and one caption-wire test (-117 net); app:browser 281→244 retires 46 legacy graph/protection/clear cases and replaces them with nine mounted atlas cases (-37 net). Core 7514→7518 adds four model-caption parser and injection-boundary tests; server 119→120 adds the authoritative-read failure ratchet. Earlier the same day, the officer-worker release gate moved core 7510→7514 for four lifecycle/audit/evidence guards and novaclaw test/* 785→795 for ten worker admission and host-bridge guards.",
  "countsLatest": "2026-09-14 accepted-exit work units: core 7533→7535 adds the exact-message durable exit projection and the goal-oriented sleep/wake/non-termination lifecycle guard; session-ui 222→225 adds the exact-message live projection, mid-run steering, and next-prompt-after-accepted-exit grouping guards; app:browser 246→247 mounts the complete visual path, including sleeping-goal busy status. Nothing moved down. Earlier, owned background shells: core 7527→7533 is +8 lifecycle guards (explicit background launch, exit refusal, deterministic nudge, ampersand classification, owned-shell wrapping, implicit-background routing, and real closed-pipe children through both Bash and Strict) minus the 2 retired configurable-shell tests; app:browser 247→246 retires the removed Terminal Shell immediate-write case; novaclaw:server 474→473 retires the removed /api/pty/shells contract with its endpoint. Every decrease is coupled to deleted product surface.",
  "countsCurrent": "2026-09-17 context-manager single seam: `core` 7263->7264 is `context-manager-single-seam.test.ts` (only ContextManager may import the context epoch). `app:unit` 1757->1756 removes the `wireSystemPrompt` case with the helper it covered: the live prompt view reads runtime state and never parses a captured body, so the wire reader is gone. The live prompt route split into `session.promptSource` (runtime baseline only) and `session.promptCapture` (the captured wire bodies, read on explicit export); the SDK was regenerated. 2026-09-17 retired inert controls: `core` 7267->7263 removes the per-model `prePrompt` and the per-session `systemPromptOverride` tests (the config-resolve inheritance pair, the fork's override materialisation, the row-inverse round trip, `tool/session`'s override projection/denial/malformed cases, and the prePrompt marker in `session-runner-model-ran`). `sdk` is unchanged in count; its naming fingerprint was re-pinned (pure removal: the prompt-override route/event schemas and the per-model `prePrompt` property, 434 deletions / 1 insertion). The `system_prompt_override` component kind, its column (migration `20260917192630_wealthy_triton` drops it), its tier/cross-read entries, the `session.next.prompt-override.switched` event, `POST /api/session/:id/prompt-override` and the composer/app plumbing are all gone. 2026-09-17 context-hygiene cleanup: the retired prompt-formation modules and their tests are DELETED, so `core` 7339->7267: `system-compose.ts`/`system-accounting.ts` and `system-compose-*.test.ts`, `session-system-compose.test.ts`, `prompt-accounting-counts-what-ships.test.ts`, `prompt-survey-contract.test.ts`; `context-template.ts` is cut to the tail table (its system-channel/volatility cases and the accounting-derivation case go with it); the durable `#DURABLE`-block and goal-block PLACE cases moved to `prompt-manager.test.ts`, and the goal AUTHORITY cases stay. `app:unit` 1760->1757 is the plan-citation ledger (a `todo/context-hygiene.md` citation I added and removed) plus `wireSystemPrompt` (+1) net of two trim; `app:browser` 253->247 deletes `settings-context-layout-render.test.tsx` with the Settings 'Where everything goes' tab and its slot table; `novaclaw:config` 144->143 is the removed `instructions` concat case; `llm` 495->497 adds `openai-chat.system-wire.test.ts` (the captured body's first message is the sent system prompt; two prepares are byte-identical). The `instructions` config key is gone from the schema, SETTINGS_KEYS, the tiers and the fixtures, so the compression suite pads with `disabled_providers` instead. 2026-09-17 context-hygiene slice (`dev`): `core` 7365->7339 (-26) is the retirement of the older system-prompt mechanisms, all of it deleted or rewritten tests: `instruction-context.test.ts` (src + test/), `session-runner-instructions.test.ts` and `session-runner-memory-stance.test.ts` (the auto-AGENTS.md embed and the memory-stance prompt section), plus the runner-level baseline claims in `session-runner-context.test.ts` (7 cases about chronological System-context updates, replaced by 3 about the one epoch-frozen prompt) and the personality half of `session-runner-agent.test.ts` (2 cases -> 1). No product test was dropped that still describes shipped behaviour: every removal is a mechanism this slice retired. The +1 back is `shell.test.ts`'s `agentPlatform` guard: the prompt's environment line now comes from `uname` in the agent's own shell, so the values cannot disagree with what the model sees. 2026-09-17 skills and Notes retirement: `core` 7469->7365 (-104), `app:unit` 1831->1760 (-71), `app:browser` 263->253 (-10) and `novaclaw:fixture` 32->31 are the deletion of the skills subsystem, the Notes app and their tests, the permission-action title family, the retired-tile contracts and one fixture refusal — every drop the retired mechanism. The novaclaw CLI help snapshot lost its `debug skill` line, and the context-layout browser suite lost its persona-editor case (identity is per-officer now). The fast-tier units all ran green after the change; `llm` 493->495 is two cases added by earlier work this session and never re-synced (the unit is green — a passing count rise, recorded here rather than left to drift). A `--full --only=novaclaw` run then lowered the full-tier bulk: `novaclaw test/*` 793->771 is the deleted `test/skill/skill.test.ts` and its 22 completed cases — the ONLY count that moved in that run, so the rest of the bulk is unchanged. That run showed two load-induced flakes, both re-run green in isolation and neither pinned (they are pre-existing, not from this change): `novaclaw:server`'s `promptAsync routes to V2` takes ~22 s alone and crossed the 30 s per-test timeout with three units in flight, and `novaclaw test/*`'s `util.process > aborting kills the child's children` is order-sensitive within its own file (alone: pass; whole file: fail). Nothing moved down that was not the retired mechanism. 2026-09-17 provider-prompt capture: core 7466->7469 is the three `prompt-capture.test.ts` cases (the render keeps system/messages/tools in one text; the first turn is kept as the init prompt while latest is replaced; a session with no capture reads nothing). The runner now writes the packed request to the agent's scratch at dispatch, and `session.promptSource` serves the two files to the Work tab and the context view. Nothing moved down. 2026-09-17 bare-JSON call appended AFTER prose: llm 490->493 is three `tool-recovery.test.ts` cases. The scan that already handled a run of calls now finds one embedded in a sentence, and requires a structured `arguments` object when it is embedded so a quoted JSON blob that merely names a tool is not executed. Found live on ses_daedalus: two turns ended `finish=stop` with a sentence followed by `{\"name\":\"bash\",\"arguments\":{…}}`, a shape every earlier fix skipped because the text did not START with `{`. Nothing moved down. 2026-09-17 whole-call-in-the-name repair: llm 478->490 is nine `splitInvocationName` cases plus three openai-chat wiring cases for a STRUCTURED call whose function NAME is the whole `bash({…})` with empty arguments — the registry could only answer \"Unknown tool: bash({…})\". The arguments are split back out, and a repeated key keeps the FIRST value because JSON.parse's last-wins would have run the human description as the command. Same daedalus run, after the text-channel fix above landed. Nothing moved down. 2026-09-17 tool-call recovery for concatenated bare JSON: llm 474->478 is the four `tool-recovery.test.ts` cases for a run of calls emitted back to back (identical repeats collapse to one, different calls survive, a brace inside an argument string does not close early, trailing prose is ignored). This was found live on `ses_daedalus` with `openrouter-ai-api-v1/stealth/union-alpha`: 9 of 12 assistant turns put the call in TEXT, and the single-value bare-JSON parse failed on the duplicate, leaving the turn `finish=stop`. Nothing moved down. 2026-09-17 background-command control: app:unit 1824->1831 adds four elapsed-formatter cases plus three activity-shortcut guards (a shell stop requires a reason and addresses the job id, both dialogs show elapsed, and both are fed live accessors); app:browser 261->263 adds the shell stop-with-reason case and a signal-driven case proving a finished command leaves an OPEN list. Both moved UP by exactly the tests added; nothing moved DOWN. 2026-09-16 ContextTemplate: core 7651->7662 is +11, the new context-template.test.ts (the table is the single source; exactly one epoch-frozen slot and it is `base`; channels do not leak into each other; the tail builder emits table order; SystemAccounting.BLOCKS still derives from the table). One existing test was RE-PINNED rather than added: todo-reminder.test.ts pinned the literal source text of a hand-written spread array that the template replaced, so it now asserts the property it is named for - the slot is DECLARED by the table and PROJECTED by the runner - plus the unchanged negative half, that the reminder is never written to the transcript as a steer. Nothing moved down. 2026-09-16 full-gate re-sync, from ONE --full run (2073 s, 26/27 units; the single red was sdk-js, a real naming-table drift nothing else could see). script 262->268 is the six new failure-excerpt tests; core 7639->7651 is +6 goal-block.test.ts, +3 net in drive.test.ts, +2 in model-taxonomy.test.ts and +1 in model-fit.test.ts; app:unit 1970->1976 is the six roster-live rootsToClear cases. Every delta is a test ADDED; nothing moved down. The sdk-js red is fixed in the same commit by reviewing the mapping: the taxonomy removed exactly one public name (ModelBenchmark, 521->520 entries) and added none, verified by diffing components.schemas across the commits. 2026-09-16 the special-model sweep, found by the owner asking \"so Special never gets picked now?\". It did not, until three more doors were closed, and this entry records the count only; the doors are in the report. core 7638->7639 is ONE added guard in test/catalog.test.ts: `default()`'s FABRICATED fallback (the \"newest released model\" arm, which runs on the first turn of every fresh install) skips a special model even when it is the newest, while a STORED default is returned whatever its class — that arm is the user's own explicit choice. Measured both ways: with the fix reverted the new test fails, restored it passes. Nothing moved down. 2026-09-16 models-taxonomy, second half: the SPECIAL class (owner: \"wont be used to power agents, unless agent settings explicitly pick it\"). core 7633->7638 and app:browser 259->260, every delta a test ADDED. core +4 is src/model-taxonomy.test.ts: special satisfies NOTHING at every rung; it scores lowest for EVERY fixed request so a leaked one cannot win a tie-break; autoSelectable is false for special and true for an UNRATED model, because absence must never read as special; and of() never materialises it from absence. core +1 is test/session-runner-model.test.ts: leastLoaded returns undefined rather than a special model even through its last-resort fallback, with the same call on a usual-rated fixture as the control. app:browser +1 is agent-config-dialog-render.test.tsx, pinning that the class a colleague may REQUIRE offers Smart/Usual/Fast and NO Special — the two vocabularies are deliberately different lengths, so the picker cannot offer a choice the schema refuses. Nothing moved down. 2026-09-16 models-taxonomy slice: core 7626->7633 and app:browser 253->259, every delta a test ADDED or a retired surface guard REPLACED. core +9 is the new src/model-taxonomy.test.ts (the single class-to-decision conversion: an unrated model IS Usual, the floor comparison, the exact-first fit ordering, and requestModel answering an empty pool); core -1 is session-runner-taxonomy-scaffold.test.ts replacing the retired six-band session-runner-tier-scaffold.test.ts (2 cases, only the Fast class is still scaffolded); core -1 is src/agent/model-fit.test.ts, where the two score-shaped silences collapse into one class comparison because absence now materialises as Usual. app:browser +4 is info-popover.test.tsx (a TAP toggles, hover opens and leaving closes, and a mouse click does not close what hover opened - the exact defect TooltipV2 shipped and explain.tsx recorded); +2 is settings-model-config.test.tsx (the class picker starts on Usual, and every field explanation sits INSIDE its title rather than on a line below). The Terminal-Bench score is gone from every unit fixture. Nothing moved down. 2026-09-14 restart-persistent worker projection: core 7548→7549 adds the database-only reconstruction guard proving a busy direct worker survives without browser state while an interrupted sibling does not; app:unit 2008→2009 adds the prompt-cache independence guard; server 121→122 adds the shared durable owned-work endpoint guard. Nothing moved down. Earlier the same day, hard device concurrency: core 7547→7548 adds the regression proving a focused interactive request cannot exceed the device's configured total generation cap while a background request is in flight; foreground work queues and claims the next safe slot. Nothing moved down. Earlier the same day, persistent owned-work senses and queued-user preemption: core 7540→7547 adds six durable worker/shell ledger, rolling-cadence, removal, spawn-purpose, positive-minute config and configured officer-wake guards plus one ratchet covering both autonomous action loops; two existing steering claims now require queued user input at the next settled reasoning/tool boundary, so their count is unchanged. App:unit 2007→2008 adds the reason-required worker-stop wiring guard; server 120→121 adds the durable direct-superior reason-delivery guard. Nothing moved down. Earlier the same day, boot recovery boundary: core 7539→7540 adds the regression proving a detached boot scan cannot adopt input admitted after startup; direct recovery still performs an immediate scan. Nothing moved down. Earlier the same day, officer order and truthful throughput: app:unit 2004→2007 adds two Nova-pinned persisted-order helpers and the Home officer-plus-worker aggregate; app:browser 249→252 adds the stored-config reorder round trip plus two worker-context dock guards. Nothing moved down. Earlier the same day, chat viewport continuity: app:unit 2003→2004 adds the saved-position deletion guard; the real-Chromium regression for returning to the same message through both another chat and Home lives in the separate E2E tier. Nothing moved down. Earlier the same day, model observability and score routing: core 7535→7539 adds two byte-prefix cache guards and two least-loaded score/capability routing guards; app:unit 2010→2003 retires the three size-tier cases and seven size-bound agent-model cases, replacing them with three score-bound model cases; app:browser 247→249 adds the default-model score removal round trip and the benchmark/prefix-cache save/reopen contract. Every decrease removes the obsolete size-rating surface, not a live behavior guard. Earlier the same day, queued-input lifecycle: app:unit 2013→2010 intentionally deletes two source-shape tests with the removed Connections popover and replaces two obsolete pending-retention tests with one cancellation-wire contract; session-ui 225→226 adds the cancellation-event projection guard. No live behavior guard was silently lost. 2026-09-15 core 7549→7556: +5 is `runner-finish-audit-retry.test.ts`, added by a2c6fa63f and never re-synced; +2 is the net replacement of `session-archived-refuses-work.test.ts` (3) by `session-prompt-resolves-filed-chat.test.ts` (5), where a prompt aimed at a filed chat is RESOLVED to the colleague's current chat instead of refused. The retired guards asserted the refusal and its successor id; the new ones assert delivery, that a colleague with no chat gets one, that a chat owned by nobody is brought back under a posture rather than a retired id, and two controls (a live chat takes the prompt in place, and Nova resolves through itself while being absent from the config store). Nothing moved down. app:unit 2009→2011 is `submit.test.ts` (+2): a prompt the kernel resolved elsewhere navigates to the session that took it and drops the echo keyed on the address, and its control proves a prompt that landed where it was sent navigates nowhere. Both units were green when this was written; only `core` and `app:unit` were run. 2026-09-15, the reach-materialises-a-chat slice: `core` 7556→7557 is `agent-reassignment.test.ts` +1, where 'SKIPPED, not queued' became 'a colleague with no chat is OPENED, not skipped' and a sibling guard was added for a name that is NOT on the roster. `novaclaw:server` 473→466 is a CORRECTION, not a test diff and not from this change: the entry had been recorded from bun's raw `Ran` line while `readTestCount` subtracts reported skips, and this unit's own `units` entry has said 7 since it was written, so 473 could never have been what the ratchet compares. Measured both ways on 2026-09-15 — clean HEAD (the whole slice stashed) and the working tree both report `Ran 473 tests across 70 files` with `7 skip`, i.e. 466 completed. Nothing moved down. 2026-09-15 compaction-overflow slice (the dispatch ceiling): `core` 7557→7567 (+10) and `llm` 498→502 (+4) and `app:unit` 2011→2012 (+1), every delta a test ADDED. `llm` +4 is `provider-error.test.ts`: the prompt-token count is read out of a provider's own context-overflow 400 (`parameter=input_tokens, value=245761` and the message-shaped forms) and a limit is only believed when the message actually states one. `core` +10 is `provider-dispatch.test.ts` (+4: the reserve the packer holds back is the one the compactor was told about, and a request that cannot fit is refused rather than sent) plus `session-compaction.test.ts` (+4: the summarizer's input ceiling is capped by the provider's own window and falls back to the whole window when no cap is configured) plus `session-compaction-decline.test.ts` (+2: a backoff declines with its own reason and does not extend the watermark). `app:unit` +1 is `debug-context.test.ts`: an over-budget request is explained as the last-moment refusal it is, naming what was dropped and that the newest message, the newest exchange and the original task are never dropped. Two pinned claims MOVED rather than being added: `session-runner-compaction.test.ts` no longer expects a dispatch that cannot fit to be attempted (requests 1→0) because the gate now names the fault instead of buying the same refusal from the provider. Nothing moved down. `novaclaw:v2` 7→4 is a CORRECTION of the same recording error fixed for `novaclaw:server` above, and not from this change: the entry was taken from bun's raw `Ran 7 tests across 2 files` line while `readTestCount` subtracts reported skips, and this unit's own `units` entry has said 3 since it was written — measured directly on 2026-09-15 with `--only=novaclaw:v2`: `4 pass, 3 skip, 0 fail, Ran 7`. 7 could never have been what the ratchet compares. All seven novaclaw units were green in the aggregate run that surfaced it. 2026-09-15 the dispatch-gate slice (the gate measured with the wrong number): `core` 7567→7569 (+2), both ADDED in `session-runner-compaction.test.ts` — the same fixture measured both ways, where a chat the provider has already counted under the ceiling is dispatched (2 requests, no compaction) and the identical chat with no usage report is refused and must be folded (3 requests, a compaction row). The claim that MOVED in the dispatch-ceiling slice above (requests 1→0) still holds and is the control. Nothing moved down. 2026-09-15 the source-release slice: `script` 250→262 (+12) and `core` 7569→7576 (+7). `script` +12 is `script/lib/arguments.test.ts`: an unrecognized argument now REFUSES instead of silently running the whole 23-unit tier, `--only core` without the equals sign is refused by name, `--force` is accepted but deliberately not advertised, and a ratchet reads `script/test.ts` so the runner cannot grow a second `process.argv` read that the validator would reject. `core` +7 is `context-pack-oversized.test.ts`: the bound for ONE message larger than the whole window keeps both ends, states the exact size withheld, refuses a message carrying media or a tool result, never cuts a surrogate pair, and — the measurement that found a real defect — the message it returns now FITS the allowance it was given. It did not: `OVERSIZED_MARKER_TOKENS` was declared as the marker's reservation and referenced NOWHERE, so the budget came from the whole allowance and the marker was appended on top of it, measuring 20,093 tokens against a 20,000-token allowance. Both deltas are tests ADDED; nothing moved down. The helper is still NOT wired — nothing in `context-pack.ts` calls it — so this is a tested first cut, not a shipped behavior. 2026-09-15 the context-management slice (`invariants.md`, Context Management clause 1): `core` 7576→7583 (+7), both files ADDED and nothing moved down. +3 is `old-context.test.ts`: the folded-away chat's name is the `stampOf` form so a listing of them sorts, the path is `<agent scratch>/tmp/oldctx-<DATETIME>.txt` and absolute, and the tombstone names the file with no placeholder surviving into the model's context. +4 is `compaction-old-context.test.ts`, which drives the REAL compactor through a REAL temp folder and then the REAL renderer: the file holds the serialized folded head (not the summary), the durable path names THAT file, the rendered checkpoint carries `<path> holds earlier chat` OUTSIDE `<summary>` and before the summary, a caller with no scratch folder gets `null` and a context byte-for-byte what it rendered before, and a write that fails withholds the line rather than naming a file that is not there. The four are separate because the two halves of the promise can each be wrong alone — a file written where the line does not point, and a line naming a file that was never written — and the seam between them is the event metadata the projector writes verbatim. 2026-09-15, same slice, owner-requested robustness: `core` 7583→7587 (+4), all ADDED in `old-context.test.ts`, which grew from the three naming pins to seven. The write moved INTO `session/old-context.ts` as `save`, so the module that owns the name owns `mkdir -p` and the write flag and no caller can pick differently — the two wrong flags are both plausible and both silent (`wx` fails a write a tombstone already promised; `a` leaves one file holding two conversations with nothing marking the seam). The four pins: the whole folder chain is created when none of it exists (no agent folder, no `tmp` — a colleague reached before its own provisioning ran, or a scratch folder deleted under a running instance); an existing file at the same name is REPLACED, not appended to and not refused; the same instant writes the same path and the newest write is the one that stands, which is why the caller cannot make a collision safe by choosing differently; and the returned path is the file it WROTE, not one it intended to write. A collision is reachable rather than hypothetical — the stamp is millisecond resolution, two folds can land in one millisecond under load, and a clock that steps backwards (an NTP correction, a resumed VM, a laptop waking) makes a NEW fold produce an OLDER name — so the test picks the timestamp and makes the collision exact instead of racing for it. 2026-09-15 compaction-can-no-longer-fail slice: core 7587→7613 (+26), all ADDED, nothing moved down. +4 is old-context.test.ts (the cut text is written as text an agent can grep: a message body survives verbatim, roles and order are preserved, a tool call and its result keep the joining id, and an image is a placeholder rather than its bytes). +7 is context-pack-dropped.test.ts (the packer returns the dropped SET, not just a count, and the two agree; a demoted system message is not reported as dropped; the tombstone is reserved before the measurement and emitted only when something was dropped; the room it costs can only ever drop more; ProviderDispatch carries the file into the system parts; and no file passed leaves packing byte-identical). +15 is session-compaction-decline.test.ts, 17→32: four declines (context-too-small, summarizer-unavailable, summary-unusable, transcript-too-large) are now deterministic fold COMMITS, the backoff folds without spending, and the new 4K..256K table (two cases per window in powers of two) plus the empty-head anti-loop guard are added. Every delta is a test ADDED; nothing moved down. 2026-09-15 the officer-model slice (the owner's *\"the picked model in the officer's settings is not respected\"*): `core` 7613→7622 (+9), all ADDED and nothing moved down. +2 is `provider-recovery.test.ts` (the reconnect ceiling is the invariant's THIRTY minutes, not ten, and capability closeness is a DISTANCE that is zero when neither side declares capabilities rather than treating silence as an empty modality set); +5 is `session-runner-model.test.ts` (rank-by-capability: distance decides before catalog order, the instance default breaks a TIE only and loses to a closer substitute, and the order is total so a snapshot cannot decide - plus the two substitution-notice wordings, where switched-off names the repair only the owner can make and unreachable promises a retry instead of sending the user to a switch); +1 is `session-runner-model-ran.test.ts` (driven against the REAL catalog: with the assigned model switched off, the farther instance default and an exactly-matching substitute both available, the turn lands on the closer substitute). And `core` 7623 is ONE more added after that (+10 total): `session-runner-model-ran.test.ts` answers the owner's follow-up by measurement - *\"as soon as the picked model gets turned on, does the agent switch back to it?\"* - the assigned model is switched off (substitution reported, reason `disabled`), switched back on, and the VERY NEXT resolution is the assigned model with no substitution. +1 is `officer-model-resolution.test.ts` (the keystone's half - a colleague's configured model resolves to THAT, not the instance default). The model picker moved to the same rule: it offers only runnable models and renders the officer's own switched-off choice labelled, and the Unattended switch and durable Goal moved from Mind to the Work tab. `app:browser` 252→253 is `agent-config-dialog-render.test.tsx` +1 (the switched-off model is not offered as a fresh pick AND the bound one is labelled rather than silently filtered, so the select cannot read 'Default Model' while the stored setting says otherwise). Every delta is a test ADDED; nothing moved down. 2026-09-15 release-notes slice re-sync (full gate, 26/27 units): `app:unit` 2012→1964 is the removal of `highlights.test.ts` and `settings-release-notes-row.test.ts` with the scrapped Updates section; `novaclaw:server` 466→459 is the removed `maintenance-changelog.test.ts`; `desktop` 240→239 is one fewer case in the consolidated `cors-scope.test.ts` ratchet. `llm` 502→474 and `novaclaw test/*` 796→793 are CORRECTIONS, not losses: both had been recorded from bun's raw `Ran` line and each delta is exactly its `units` skips (28 and 3), the same recording error already corrected for `novaclaw:server`. `session-ui` 226→229 is carried from commits after the last re-sync. Nothing moved down unintentionally. 2026-09-16 models-and-scheduler: app:unit 1964→1968 adds three switched-off-model resolution guards plus the Scheduler-tab guard; core adds the carryless-admission and warm-cohort scheduler regressions. Later the same day, officer-model re-point: the guard landed first (app:unit 1968→1970, two ratchets), then the single-source migration REPLACED it — the model shadow and its two stopgap ratchets are gone, and app:unit settles at 1968 with five guards on the session-owned override. Nothing moved down unintentionally. 2026-09-16 the prompt-layout screen (owner: \"clearly exposing the layout in UI, while allowing user to both view and edit it\", plus \"ensure that the template given static arguments, gives static results - i.e. it is deterministic and respects the prefix cache\"): app:unit 1976->1980 (+4) is `context-layout.test.ts`, the drift ratchet between the kernel's slot table and the screen - every slot has a provenance entry and no entry is orphaned, the two maps are equal in LENGTH and not merely name-matched, every slot's name/origin/channel/volatility copy resolves in English, and the three slots a plausible edit mislabels (`base` is files, `projectScope` is the folder, `goal` is the agent) are pinned by name. app:browser 260->266 (+6) is `settings-context-layout-render.test.tsx`, the same claims read off the DOM: 21 rows in kernel order, no row blank (a missing key renders as \"\" through `languageStub`, exactly as the app does), the freeze is stated on `base` and a per-turn slot says the opposite, the mislabel guard again on screen, and the persona textarea still the live editable control. core 7662->7666 (+4) is `context-template.test.ts` and is ARITHMETIC rather than a whole-unit observation: that file ran 15 (up from 11) when the four determinism guards were added - identical arguments produce byte-identical output, ORDER comes from the table and never from the argument object's key order, the tail builder is order-independent, and a source ratchet that the module reads nothing but its argument (no clock, no random, no env, no imports at all), because every one of those would silently change the request prefix and pay a full re-prefill of the history behind it. Nothing moved down. 2026-09-16 the DURABLE AREA (owner: ensure Where everything goes lists the new durable area with items set by durable_set and durable_clear): core 7666->7678 (+12), all in the new src/session/durable.test.ts. Twelve cases: the codec limits (30 for a name, 512 for a value, and no newline in EITHER half because the area renders as LINES and a value must not be able to forge a second item), the id-is-the-slug-of-the-name authority check, the host-only materialised block, keyOf folding two spellings of a name to one slot and giving a symbol-only name a stable distinct slot, the insertion-order independence of the render, malformed rows dropped rather than thrown, each refusal naming its remedy, and the slot position (immediately after goal, the only compaction slot, on the system channel). Two EXISTING ratchets were RE-PINNED rather than weakened, and they are the record of this landing: context-template.test.ts had a case whose own comment said the compaction value is what the durable area needs, and that when the durable slot lands it must carry compaction rather than turn or a mid-run durable_set churns the prefix - that case asserted the column was UNUSED, and it now asserts the column PARTITIONS the table (epoch = base only, compaction = durable only, the remainder per-turn) so a second materialised slot still has to declare itself; its system-order expectation gained durable after goal. test/session-system-compose.test.ts is a LEDGER whose Required/Omit type fails to typecheck until a new part is classified, so durable was classified OPTIONAL with the reason (absent whenever the shadow copy is empty, which is every session that never called durable_set). Nothing moved down. app:unit (1980) and app:browser (266) were run whole and are UNCHANGED: the layout screen picked the new slot up through its own drift ratchet, which forced a seventh provenance kind (session - the chat writes it through its own tools, so neither the colleagues settings nor supplied-automatically is true) plus two labels, and both app tests were extended in place rather than added to. 2026-09-16 the PROJECT-FILE RETIREMENT, test pass: core 7678->7475. Every drop is a case DELETED because its subject was the retired mechanism, and the two directions were kept apart deliberately. DELETED: the project-file resolve/write/cache/exclusion/gitignore/defaults suites (whole files), permission-project-file, folder-stance, project-defaults-entry-point, the folder skill-hiding suite in skill-invocation-command-list (7 cases), the folder-declared-policy cases in tool-policy-management (2), what-a-novaclaw.json-can-buy in tool-policy (3), the exclusion cases in tool-bash, tool-edit and instruction-context (4), the REPAIRED ledger entry in invisible-characters for project-exclusion.ts (1), and the three HTTP project route files. ADDED: 12 cases in the durable-area suite built earlier this session. Two tool files were split out of one (durable-set.ts and durable-clear.ts register one tool each because the deferred-manifest generator is one-tool-per-file), which added no cases but did require regenerating the deferred manifest and classifying three files plus two generated registrations in the untrusted-framing ledger. RE-PINNED rather than deleted, each with its reason in place: the withPermission identity guard (its glob/grep arms were failing on CRLF a PowerShell Set-Content introduced - the guard compares exact lines and git status cannot see it, because autocrlf normalises the index), the memory-ceiling source claim (the fold it named is gone; the nesting it asserts is not), and the framing ledger. Nothing moved down that was not about the mechanism. core was run whole once (7475 observed; the single failure was the framing ledger's new tool files) and that failure's file was re-run green after the fix, so the count here is a whole-run observation. 2026-09-16 the same retirement, APP-SIDE pass: app:unit 1980->1833 and app:browser 266->262, every drop a case DELETED because its subject was the retired mechanism - the project API client, the folder summary/indicator, the composer's make-default section, the folder policy editor, the folder skill-hide control, the project i18n families' reachability cases, and the folder-provenance cases of config-provenance (its session half survives). Two ledgers were LOWERED rather than silenced (the settled-resource counts for policies.tsx and skills.tsx, and the two deleted files' lines removed) and three now-unreachable `.more` strings were deleted from en.ts, which is the row-copy ratchet doing its job. 🔴 FOUND BY MEASURING, not by reading: `test-browser/` is NOT in packages/app's tsconfig, so a stale import there cannot fail `typecheck:app` - it makes bun drop the WHOLE FILE from app:browser, so the tier reported 259 pass / 0 fail while a suite of 3 live cases had silently vanished. Caught by asking why a count moved when no browser test file was deleted. After fixing the import: 262 = 266 - 4 deleted project cases. Nothing moved down that was not about the mechanism. Same pass, the units the retirement reached beyond core and app: sdk-js 46/0 (the generated spec was stale, regenerated, and the public naming fingerprint re-pinned AFTER reviewing the mapping - 479 deletions and zero additions, every removed name a Project one, recorded at the constant), session-ui 229/0 unchanged, and novaclaw:server 459->418 (three cases of GET /api/policy asserted the FOLDER half - what a folder asks for, whether a request is missing or switched off, and the file it named - deleted with tombstones because a folder can no longer ask for anything). ⚠️ NOT yet run with this work, and their counts are likely stale for the same reason: novaclaw test/*, novaclaw:config, novaclaw:tool, novaclaw:control-plane, novaclaw:fixture, schema (its project-file test file was deleted), desktop, llm, protocol, ui, script, host, effect-drizzle-sqlite, http-recorder. RAN SINCE: schema 105->61 (its project-file test file was deleted; 0 fail) and desktop 239/0 UNCHANGED. 2026-09-16 test-browser joins the app's typecheck: app:unit and app:browser counts are unchanged (1980->1833 and 266->262 from the retirement pass), and 13 type errors that had never been typechecked were fixed - one was mine (a browser test still passed `project` to the control state), twelve were pre-existing looseness in files the tsconfig never included (a `mount(() => Element)` that had to take JSX.Element, two fetch-stub casts, and two `providers` reads through a store typed {}). A FULL typecheck then found a compile error my own brace-matching script had left in packages/novaclaw/test/server/httpapi-policy.test.ts - a half-deleted `it.instance(` whose name sat on its own line - which no unit caught because bun runs those files without typechecking them; repaired, and the file runs green. ⚠️ `novaclaw:server` 418 -> 419 is recorded as UNEXPLAINED, deliberately. The same commit took that file from THREE case openers to TWO and the unit fails nothing, so the count rose while a case left. The half-deleted `it.instance(` at HEAD is the obvious suspect (a call whose first argument was an Effect instead of a name), and the honest state is that I did not establish it - this is a flag for the next measurement, not an attribution. app:unit 1823 -> 1824 is ONE new test: the retired-noun ratchet (app/src/retired-project-noun-ledger.test.ts), which measured 182 remaining mentions across 91 files in 6 packages and fails if any package count rises or falls without the baseline being lowered with it. 2026-09-17 the retired project layer leaves the kernel, and its two counts are EXPLAINED deletions. core/skill/invocation.ts no longer takes a projectHidden parameter (documented against a module the retirement deleted, with nothing left able to supply it), Visibility and ShownBy lose their project arm, and the app view collapses with it: hiddenByProject, effectiveMe and inForce are gone because with one layer they could only ever repeat me and preset. core 7475 to 7466 is -9: 3 rows out of the visibility precedence table plus 6 cases whose subject was the layer itself. app:unit 1833 to 1823 is -10: the folder describe block held 11 cases and one property was KEPT, now its own describe (the two switches are not twins - the skills store must not change what agents may choose). The core unit came back SHARDED and degraded with one unconfirmed failure (trash store, nested content); that file run alone is 17 pass 0 fail, so the wedge is the shard, not the code. 2026-09-17 the retired noun leaves the SURFACES derived from it, found by sweeping the noun rather than the retirement list: the context-layout page labelled its projectScope slot with a project file that no longer exists (SystemCompose.projectScopeSection generates that section from the permission mode), a verbatim quotation a quotation in core receipt.ts attributed to an AGENTS.md principle turned out to be in no principle, note or doc in the plan repo and was removed rather than re-attributed, and three test comments plus one fixture still sent the retired source kind. app:browser 262 to 261 is UNEXPLAINED and recorded as such: files stayed at 47, failures at 0, an A/B with these changes stashed observed 261 too (so not this cleanup), and no case in the tier is marked skip or todo. Candidate, not a measurement: the last commit that touched that tier. 2026-09-16 the provenance noun leaves the types: Resolution.project in core, the project arm of the server's DefaultSource union and the wire's project field, the app's project origin arm and its label, plus three test sites. server 122->120: two cases deleted with tombstones (a component the folder supplied names the file; a real novaclaw.json reaching the wire) and ONE RE-PINNED rather than deleted - the folder-outranks-colleague case, whose property is that the layer which WINS the fold is the one NAMED, now naming the colleague.",
  "countsLearnedRecovery": "2026-09-18 learned endpoint recovery: `core` 7264->7276 is +12 tests, every delta ADDED and attributed; nothing moved down. Session affinity (+7): `provider-session.test.ts` (5) pins the brand-free learn-from-the-endpoint's-own-400 seam and the header value rules, and `session-runner-model.test.ts` adds two (the conversation id reaches the route defaults once the header is learned; an endpoint with no learned header is left alone). The learned repetition floor (+5): `repetition-floor.test.ts` gains three (the endpoint's own refusal is recognised and nothing weaker; endpoint identity normalizes and a malformed URL is never remembered; a refusal is remembered for exactly that endpoint) and `session-runner-model.test.ts` adds two (the floor is omitted for the persisted flag and for this process's own memory; a different endpoint keeps it). The 2026-09-18 session-affinity refactor replaced the compiled host/header check with per-endpoint learning: `core` 7269->7271 is +2, both ADDED and attributed — `provider-session.test.ts` 5->7 adds the brand-free learn-from-the-endpoint's-own-400 predicate/extraction and the endpoint-keyed memory cases; `session-runner-model.test.ts` (22) and `repetition-floor.test.ts` (6) renamed cases in place with no count change.",
  "countsCalendarOverrides": "2026-09-18 calendar override retirement: the per-schedule model, folder and permission-mode overrides are GONE from the Calendar app, the wire, the DB (migration 20260918014107_drop_calendar_overrides) and the scheduler, so every affected unit MOVED DOWN and each decrease is coupled to deleted product surface. `core` 7276->7269 (-7): `execution-settings.test.ts` 11->7 loses the four model-only cases (missing provider, unknown model, switched-off model, and the agent-before-model ordering), and `scheduler.test.ts` 21->18 loses the explicit per-task location, the duplicate per-task folder case, and the per-schedule model+permission resolver which is replaced in place by an agent-only case. `server` 120->114 (-6): `calendar-recurrence-domain.test.ts` 9->5 retires the whole permission-vocabulary block (the closed set no longer exists on the wire), and `calendar-location.test.ts` 4->2 keeps only the ambient-roster create/update checks now that no schedule can pin a folder. `sdk-js`, `novaclaw:server`, `app:unit` and `protocol` are unchanged in count; the SDK artifacts were regenerated (pure removal of the model/location/permissionMode properties).",
  "countsEndpointNormalization": "2026-09-18 endpoint normalization: `core` 7271->7285 is +14, all ADDED and attributed — `config-endpoint-url.test.ts` pins the lay-user URL shapes (scheme completion, `/v1` append, pasted `/v1/chat/completions` and `/v1/vectorize/upsert`), the competing-harness caveats (never append `/v1` beside an existing version; never truncate past a version marker; longest-first operation stripping; bare-host scheme defaulting), the conservative `stripped` fallback used when nothing could be probed, the refusal cases, and `modelListCount`. `novaclaw:server` 419->428 is +9, all ADDED: `provider-probe-discovery.test.ts` pins the selection rules over the real transport — canonical-first, version-less fallback, a non-list 200 conceding to a real list, a real list beating an auth challenge, an unreadable address refused before any socket, and an airgap refusal stopping the loop. `app:unit` 1756->1757 is +1: `dialog-new-model.test.ts` pins that the Add dialog adopts the URL the probe actually used rather than the line the user typed. Nothing moved down.",
  "countsConsentRetirement": "2026-09-18 consent machinery retirement: `core` 7286->7283 is -3, every decrease coupled to deleted product surface. The durable saved-grant table, its service, `permission.saved.list`/`.remove` and the ask evaluation route are GONE (migration 20260918045440_drop_permission_table), so `permission.test.ts` loses 'uses saved bash approvals...', 'stores and removes saved resources for a project', and 'a saved allow-always DOES unblock it'. Two assertions were rewritten in place with no count change: attachment protection is no longer released by a saved answer, and the hard stance is proven with a standing configured rule. `sdk-js` is unchanged in count; its schema-name fingerprint was re-pinned 515->503 (pure removal: two schemas and three paths), and `novaclaw:server` 428 and `server` 114 are unchanged. The migration drops the `permission` table; `db-registry` no longer names it.",
  "countsCardExpansion": "2026-09-18 card expansion and todos: `app:browser` 247->249 is +2, ADDED — `native-transcript-remount.test.tsx` pins that an unfolded command survives the next agent step (expansion moved above the `Turn`, so the step's remount cannot close it) and that `todowrite` is folded by default because the composer dock already carries the list. Nothing moved down.",
  "countsComponentOwnership": "2026-09-18 agent owns its components, and the mode switch is immediate: `core` 7285->7286 is +1, ADDED — `drive.test.ts` pins that `SessionDrive.decide` reads the ROLE's `operationMode` (through `unattendedMode`), so switching a colleague between Interactive and Unattended lands without the UI's second `switchType` request and without a sleep that waits for the next prompt. Nothing moved down. The component WRITE tiers and their `session`/`session_privileged` charge are gone; `memo_set`/`memo_clear` and the generic `session` tool no longer charge, and the only limits on what an agent may change are the hard `system`-only gates on personality (`agent`), `goal`, project (`working_folder`), `session_type`, `responder`, `durable_prompt` and plan verdicts. `tool-session.test.ts` and `session-component-kinds.test.ts` were rewritten in place (write-tier assertions swapped for cross-read and authority-gate assertions) with no count change.",
  "countsSlashAtRetirement": "2026-09-18 /command and @context removal from the prompt editor: `app:unit` 1757->1742 is -15, every decrease coupled to deleted product surface. DELETED: `popover-controller.test.ts` (2), `placeholder.test.ts` (4), `placeholder.ts`, `popover-controller.ts`, `popover-options.ts`, `slash-popover.tsx`. REMOVED cases: `build-request-parts.test.ts` loses the file-attachment, agent-attachment, dedupe-against-prompt and @mentions-in-comments cases; `prompt.test.ts` loses the @-file weave, @-agent weave, selection-range and relative-path cases; `editor-core.test.ts` loses the pill round-trip, createPill and @-trigger insert cases (replaced by a plain text-insert case). The composer placeholder is now always `Ask anything...` (shell mode keeps its own). Nothing added.",
  "countsPerAgentTuning": "2026-09-18 per-agent tuning slice 1 (schema + resolution, no UI, no removal): `core` 7283->7303 is +20, all ADDED and attributed — `session/officer-harness.test.ts` (15) pins the officer<session field-wise merge for Strict, affective/introspection normalization and resolution, the narrow-only tool horizon (configure never denied), and the by-name recipe merge with the globalTools opt-out; `agent-defaults.test.ts` (+5) pins full Strict detail folding, the bool-or-struct stance/detail split (a `{enabled:false}` struct must not read truthy), detail-without-stance inheritance, and the tools/adhocTools/globalTools fold. `app:unit` 1742->1750 is +8, all ADDED — `apps/agent-settings-copy.test.ts` pins the tuning-only copy (identity/work never cross, list replacement flagged, absent never clears, fragment aliased from source, schema-minus-NOT_COPIED accounting with negative control). `sdk-js` is unchanged in count (46); the SDK artifacts were regenerated for the widened officer/record schemas (Strict detail, bool-or-struct stances, `tools`). Nothing moved down.",
  "countsPerAgentTuning2": "2026-09-18 per-agent tuning slice 2 (runner wiring): `core` 7303->7315 is +12, all ADDED and attributed — `session/officer-harness.test.ts` (+4) pins `chainDeclared` (same reference means the chain said nothing; equal-by-value still counts as declared) and the no-officer equivalence (`resolveStrict` without an officer IS the old whole-object spread); `runner-config-per-turn.test.ts` (+5) pins `HarnessConfig.withOfficer` (identity without an officer, Strict/affective/introspection detail overlays, empty structs normalize to absent, per-turn members ride along); `adhoc-guidance-session.test.ts` (+3) pins the officer recipe layer (private recipes list, same-name hide, `globalTools: false` drops the library). The store-root ledger was re-pointed at the delegation (`tool_manual` resolves through `forSession`, pinned by name). One `core` run died environmentally mid-shard (EPERM rename in `kb-graph` snapshot publish at 79% commit with a wall-clock kill reaping orphans; the file passes 30/30 alone); the clean re-run is green on both shards. Nothing moved down.",
  "countsPerAgentTuning3": "2026-09-18 per-agent tuning slice 3 (officer UI): `app:browser` 249->255 is +6, all ADDED and attributed — `agent-config-dialog-render.test.tsx` gains the Strict tab merged-struct save (attempts without wiping levers), the Affective bare-boolean-to-struct upgrade, the Introspection detail display plus confirm-gated reset-to-inherit, the Tools live deny plus private-recipe add, and the two copy-tuning cases (identity/work never cross; private lists confirm before replace). The pre-existing layout test was re-pinned to the new tab homes (Mood sampling and Stuck detector out of Mind/Work). `app:unit` is unchanged in count (1750). Nothing moved down.",
  "countsPerAgentTuning4": "2026-09-18 per-agent tuning slice 4 (global tabs removed; instance keys deleted). `app:browser` 255->245 is -10, every decrease coupled to deleted surface: `settings-affective-write-count.test.tsx` (-6) left with the Affective Settings tab, and `settings-number-field-commit.test.tsx` 9->5 (-4) lost its Strict cases — that tab's number field was a live-write per-keystroke clamp, and the officer Strict tab's attempts box is a Save-time draft, which cannot coerce mid-typing by construction. `app:unit` 1750->1752 is +2: `components/agent-settings-tabs.test.ts` pins rail ids == CSS reveal rules in both directions, after four new officer tabs rendered live in the DOM while the shipped stylesheet hid them all (happydom does not apply stylesheets the way the product does, so no render test could see it). The three tests that had silently stopped registering are restored, not added: `models-enablement.test.ts` read the deleted `settings-v2/introspection.tsx` at describe scope, so its ENOENT dropped 3 tests without a failure line. `core` 7315->7311 is -4, both parts coupled to deleted surface: `src/session/officer-harness.test.ts` 19->16 is `resolveRecipes` DELETED as unreachable once the instance recipe library went (its tests left with it, which is the ratchet working), and `test/adhoc-guidance-session.test.ts` 7->6 is the `globalTools: false` case, which has no subject now — there is no library layer to opt out of. Verified stable across a sharded run and a whole run, both reporting 7311. Nothing moved down that is not coupled to deleted surface.",
  "countsPerAgentTuning5": "2026-09-18 per-agent tuning, live verification follow-up: `app:unit` 1752->1753 is +1, ADDED — `components/agent-settings-tabs.test.ts` gained 'every section is reachable', which catches the mirror of the defect its sibling pins: a section tagged with a `data-settings-tab` id no rail entry can select is PERMANENTLY hidden, exactly as a tab with no CSS rule is. Nothing moved down. The rest of this follow-up is live evidence rather than counts: see notes/reports/per-agent-tuning-2026-09-18.md, 'Live verification'.",
  "countsUnattendedNamedWorkspace": "2026-09-19 unattended named-workspace fix: `core` 7311->7313 is +2, both ADDED and attributed to `permission.test.ts` — an unattended root may write a folder its own floor NAMED (a colleague's scratch survives the confinement stance), and a named workspace does not reopen Analyze. The fix itself is in `permission.ts` (`evaluateInput` re-asserts the agent's specific `external_directory_write` allows after the stance; a blanket `*` is filtered out and still refused, pinned by the pre-existing standing-allow case). Nothing moved down. One environment run died on the known `kb-graph` snapshot `EPERM` rename (documented in countsPerAgentTuning2); `snapshot.test.ts` passes 23/23 alone.",
  "countsRestartNudgeAndOneAxis": "2026-09-19 one restart nudge, and no horizontal scrollbar. `core` 7313->7311 is -2, coupled to deleted surface: the recovery branch no longer needs to name interrupted child waits, because each interrupted call names itself (`<tool-name> interrupted, result unknown.`), so the child-wait special case and its `recovery-join.ts` + `recovery-join.test.ts` (2 tests) are gone. No core test was added: the three recovery/blocked-tool files were re-pointed to the new single nudge and the per-tool message in place. `session-ui` 229->231 is +2, ADDED — `native-transcript-width.test.ts` pins the transcript root wraps and clips (`min-width:0`, `overflow-x: clip`, `overflow-wrap: anywhere`) and that no transcript sheet re-adds `overflow-x: auto`. `app:unit` 1753->1755 is +2, ADDED — `timeline/native-timeline-width.test.ts` pins the chat scroller clips its horizontal axis (`overflow-x-hidden`), because `overflow-y-auto` alone computed `overflow-x: auto` and one long denied-tool path drew a bar across the chat, and pins the shell-wide `::-webkit-scrollbar:horizontal` rule that keeps any other surface from painting one. `app:browser` is unchanged in count. Nothing moved down except tests deleted with their subject.",
  "countsMemoDeferredUntilRewrite": "2026-09-19 memo writes defer to the rewrite again. `core` 7311->7317 is +6, all ADDED and attributed. The fix restores the two-component split commit `4c1cdc20c` built and `139750f57` lost: `renderPrompt` reads the materialised `durable_prompt` singleton via `Durable.textOf`, not the live `durable` shadow, so a `memo_set`/`memo_clear` no longer changes the system prompt on the next turn and no longer trashes the provider's prefix cache; the block moves only when a compaction materialises it. `test/session-runner-agent.test.ts` (+1) is the integration claim — a `durable` write leaves the prompt byte-identical until a `durable_prompt` materialisation, then the area appears. `src/tool/memo.test.ts` (+5) is new and pins the refusals the owner asked for: the eleventh memo says the area is full and names `memo_clear NAME` before the existing menu; an over-long name names the 30-character limit; an over-long value names the 512-character limit and points at a file; a name reused at the limit REPLACES rather than refusing; and a successful write says the memo appears only after the compaction. Every delta is a test ADDED; nothing moved down.",
  "countsContextTabAndCompactionThreshold": "2026-09-19 Context tab, draggable split, and a configurable compaction threshold. `core` 7317->7317 is net ZERO, both halves attributed: `TaxonomyScaffold` (the `fast`-class prompt paragraph, dead since the 2026-09-17 one-prompt cleanup) is DELETED with its 2-test file, and `session-compaction.test.ts` gains 2 tests for the new percentage trigger (`settings([]).threshold` defaults to 80; `triggerAt` takes the earlier of the percentage and the response-reserve ceiling, and no percentage can raise the trigger above the ceiling the packer must respect). `ConfigV2.Compaction.threshold` (1..100, default 80) is wired into both trigger sites; the SDK was regenerated (`ConfigV2Compaction.threshold`). `app:unit` 1755->1761 is +6, ADDED — `parts/context-allocation.test.ts` pins the boundary transfer (sum preserved, clamps at zero, out-of-range is a no-op) and asserts the app's default mirror key-for-key against the kernel's `ContextBudget.DEFAULT_PROFILES`, so the two cannot drift. `app:browser` 245->248 is +3, ADDED — `settings-number-field-commit.test.tsx` mounts the Context tab and pins the renamed title and guard copy, four five-part bars (four drag handles each), the arrow-key commit as one config write, and the compaction threshold committing through the shared number field with the shipped 80 shown. Nothing moved down except tests deleted with their subject.",
  "countsNotificationPreviewAndDefaultPermissionMode": "2026-09-19 notification preview and the retired default-permission-mode setting. `app:browser` 248->250 is +2, ADDED — `notifications-list.test.tsx` pins that the shared rows honour `RECENT_NOTIFICATIONS_LIMIT` (the Settings preview shows the newest five) while the unbounded list keeps every entry, and that an empty history renders one 'no recent notifications' row rather than nothing. `app:unit` and `core` are unchanged in count. The removal itself: Settings → General's 'Default permission mode' row, its `defaultPermissionMode` store field/default/accessor, `permissionModeOptions`, and the three `settings.general.row.defaultPermissionMode.*` i18n keys are gone, plus the `row-copy.test.ts` SPLIT entry. `context/local.tsx` no longer reads the setting; an unset new-session draft now starts on the KERNEL default (`EFFECTIVE_CONFIG_DEFAULTS.permissionMode`, 'bypass'), which is what the removed setting's own default was — so behaviour for anyone who never touched it is unchanged, and a per-device client default for a server-held posture is no longer a second place to disagree with the kernel.",
  "reasons": {
    "core": "9 as of 2026-09-05: eight Windows PTY cases and one symlink watcher case. Commit 323345771 re-enabled the two offline file-fixture npm cases, but left the old eleven-skip pin. Core count is 7478 after the 2026-09-09 recovery, lifecycle, single-terminal-door and officer-owned worker-cascade regressions were added. Lowered to 7445 on 2026-09-10 because the finish re-ground drive was retired: its unit file and the drive's per-arm cases left with the feature. A count that drops because tests were REMOVED is the ratchet working, not a regression — the llm reason below is the same shape.",
    "llm": "28 as of 2026-09-01, lowered from 29. The refactor sweep deleted the unused provider surface and its tests (`test/exports.test.ts`, `test/provider/cloudflare.test.ts`, `test/provider/openrouter.test.ts` and others); one of those files carried a skipped test, so the skip left with the file it lived in. A skip count that drops because tests were REMOVED is a ratchet working, not a regression — but it is lowered deliberately and in the same commit, never left to drift.",
    "novaclaw:server": "NO pins — this unit is expected FULLY GREEN, and any failure is drift. The empty `failing` entry is documentary: it is inert to the matcher (a pin needs a non-empty set) and exists only so this reason has a unit to hang on. ⚠️ A flake belongs in quarantine, never here — this unit has produced different new failures on consecutive runs under memory pressure, and an environmental failure pinned as expected is an expectation that can never come true.",
    "novaclaw test/*": "Full release tier measured 780 on 2026-09-06, plus the metadata queue, stale-generation event-fence, and large image worker-transport regressions. Separate CLI/MCP leaves are excluded from this aggregate by the runner and counted under their own unit names. The headless run leaf moved 15→17 on 2026-09-12 for two added guards: a resumed session follows its stored location, and a named colleague remains the executing agent while each one-shot task stays an independent child thread. Nothing moved down."
  },
  "countsCompactOfficerUiAndRecovery": "2026-09-20: app:unit 1761->1778. The previous two rate-formatting commits already measured 1766 without updating this ledger. This slice adds net 12: +1 portrait initials, -1 retired review-tooltip helper, +5 transcript recovery, +5 reconnect lifecycle/SDK stream, +2 cancellation deadline. app:browser 250->254 adds +1 officer settings keyboard orientation, +1 model settings draft retention, +2 focused-tab/nested-dialog Escape regressions. The tab-retention browser case now exercises the seventh officer against the intentional six-tab limit. No tests were skipped.",
  "countsRecoveryStateDiscriminator": "2026-09-20: app:unit 1778->1779 adds one guard that an internal settlement handoff without a failure classification cannot masquerade as failed-worker recovery. Classified automatic recovery remains visible. Nothing moved down.",
  "countsOpenAiMissingUserQuery": "2026-09-20: llm 497->498 adds one exact-wire guard for a colleague delivery followed by a local tool exchange when the transcript contains no owner message. OpenAI Chat and the OpenAI-compatible route now insert one provenance-marked harness query before the conversation; ordinary requests with an existing user turn are unchanged. Nothing moved down.",
  "countsOfficerTiles": "2026-09-21: app:unit 1779->1781: prior ba3fce31f removed 7 tests without updating this baseline; this slice adds 4 settings-tab routing and 5 clear-chat replacement tests. app:browser 254->260 adds 5 pointer-sensor behavior tests and 1 settings-header action test. Existing roster test now exercises context menu and touch hold. No new skips.",
  "countsPhoneOfficerScroll": "2026-09-21: app:browser 260->261 adds one pointer behavior test proving vertical touch movement remains scrolling while horizontal dragging stays available. No tests removed or skipped.",
  "countsProbeSessionAffinity": "2026-09-21: the Settings generation Test now carries the endpoint's learned session affinity, so a gateway the runner already knows about is diagnosed the same way. `core` 7367->7371 is +4, all ADDED and attributed to `provider-session.test.ts`: the persisted row is read with this process's memory winning over it, a write merges the sibling endpoints rather than replacing the map, a malformed URL is neither read nor written, and a source ratchet proves the row has ONE reader/writer in `core/src` (`provider-session.ts`) so a third request site cannot reopen it and disagree with the runner. `novaclaw:server` 428->432 is +4, all ADDED to `httpapi-provider-probe-completion.test.ts`: the probe learns the named header from a `MissingSessionID` 400 and retries it once under the session-less identity, an already-learned endpoint sends the header on the first request, a refusal that is not the session gate is reported rather than retried, and a second `MissingSessionID` after the named header is reported rather than looped. Nothing moved down.",
  "countsPerModelApiChannel": "2026-09-21: a model may now pick its API channel, and the Settings probe speaks it. The Go gateway is mixed-protocol — measured live: `muse-spark-1.3-contributor` answers 503 on `/chat/completions` and 200 on `/responses`, while `glm-5.3-flash` is the reverse — so a provider-wide channel cannot express it. The runner already dispatched on the resolved model's `api.package`; the probe did not, so its Test read a 503 as a dead endpoint. `core` 7371->7373 is +2, ADDED to `provider-capability.test.ts`: the `openai-responses` wire joins the envelope-refusal matrix and the exhausted vocabulary (`incomplete`/`max_output_tokens`), Responses text is joined across message parts, and a `function_call` item carries its arguments as a JSON string for the one argument reader. `novaclaw:server` 432->439 is +7, all ADDED: `httpapi-provider-probe-completion.test.ts` gains the `/responses` route with its output-envelope validation and a chat-shaped body read as a format failure; `httpapi-provider-probe-capabilities.test.ts` gains a five-case responses block (flat tool shape and `text.format`, a native `function_call` verdict, a spent reasoning pass as a budget fault, the prompted rung, and a chat-shaped body as malformed). `app:unit` 1781->1783 is +2, ADDED to `dialog-model-config.test.ts`: the API-type list with a human label per option, and the override written onto the model's `api` with `inherit` removing it. `app:browser` 261->262 is +1, ADDED to `settings-model-config.test.tsx`: the picker round-trips `@ai-sdk/openai` onto the model and `inherit` removes the override. Nothing moved down.",
  "countsOrphanedJobRows": "2026-09-22: a stopped session settles the durable rows of the background jobs whose process died with it. Owner report: clearing a chat left its `find` job `running` with `time_done: null` and no process behind it (measured live, `job_0c610a806001fUln4ORT5IPlmW`, owner `ses_geryon`), so the commands list showed a phantom and the manual stop was a silent no-op — `stopCommand` only signalled a LIVE worker, and the job's own finalizer ran in the worker process that was killed. `core` 7373->7375 is +2, ADDED to `bash-jobs-durable.test.ts`: `stop` settles a stale `running` row (and it leaves `listRunning`), and `interruptSessions` settles the named sessions' running rows while leaving a settled row and a sibling session's live row untouched. `novaclaw:server` is unchanged in count; `src/session-worker/execution-routing.test.ts` (full tier) gains one source ratchet proving the interrupt path and the no-live-worker stop both settle. Nothing moved down.",
  "countsArchiveStopsWork": "2026-09-22: archiving a session now stops its work, the invariant's other half. `V2Session.setArchived` only patched the row, so an archived chat's workers and shells kept running — and `time_archived IS NULL` is the \"live root\" predicate, so the product's own clear/yield path archived precisely to make way for a successor while the predecessor worked on. It now interrupts the WHOLE tree before patching (awaited, so the archive the caller is told succeeded is one whose work is already gone) and evicts the scheduler residue, exactly as removal does; and both drains refuse to start for an archived session, so a queued input, nudge or recovery adoption cannot resurrect it. `core` 7375->7378 is +3, ADDED to `session-archive.test.ts`: archiving interrupts the session, archiving evicts it from the device ledger (a queued turn cannot dispatch), and a non-archiving call interrupts nothing. `novaclaw:server` is unchanged in count; `src/session-worker/execution-routing.test.ts` (full tier) gains one source ratchet pinning the drain's archived guard. Verified beside it: session-create 30, session-prompt-resolves-filed-chat, session-one-chat-per-agent and spawn-wakes-child (92 across the four) all green, so the one-chat-per-agent and folder-move archive flows still pass. Nothing moved down.",
  "countsLearnedWire": "2026-09-22: the generation Test learns the wire a model is actually served on when the configured one is refused. Owner report: they re-added `muse-spark-1.3-contributor` and Test still failed — the model carried no channel override, so the probe asked `/chat/completions` and the gateway answered 503 while serving the model only on `/responses`. A provider-wide channel cannot express a mixed gateway and nothing but the endpoint can say which wire a model is on, so the refusal is now the evidence — exactly as it is for the session header beside it. `probeCompletionLearningWire` tries the configured wire, then the other two, bounded; only an `error` refusal counts (auth and a dead socket do not); the learned package is persisted onto the SAVED model's `api` through `ConfigStoreWrite.apply`, so the Test and every later turn agree, and an UNSAVED endpoint never learns. `novaclaw:server` 439->444 is +5, all ADDED to `httpapi-provider-probe-completion.test.ts`: a refused wire finds the served one and names it, a working configured wire is not second-guessed, auth is not a wire problem, an unsaved endpoint tries only its configured wire, and every wire refused is bounded at three requests. `core` 7378->7379 is +1, ADDED to `config-catalog-reload.test.ts`: the learned channel, written in the Configure picker's shape, re-materialises the live catalog so the runner's `api.package` flips to `@ai-sdk/openai` — the Test cannot pass while the next turn goes out on the refused wire. Verified live against the gateway through this helper: `muse-spark-1.3-contributor` on the chat wire returns `ok` on `/responses` (`@ai-sdk/openai`, 2 attempts), and `learn: false` returns the faithful failure. Nothing moved down.",
  "countsDecodedConfigWrite": "2026-09-22: the learned-wire write shipped a 500 and is fixed. `ConfigStoreWrite.apply` ENCODES its argument to store it, so it takes a DECODED `Config.Info`; the probe passed a raw literal and the server answered `SchemaError: Expected Config.Info` (measured live from the instance's server.log, at `ProviderHttpApi.probe`). `PATCH /config` decodes through the HttpApi and every test decodes through `decodeInfo`; the probe now does `Schema.decodeUnknownSync(ConfigV2.Info)` too. `core` 7379->7380 is +1, ADDED to `config-catalog-reload.test.ts`: a NEGATIVE CONTROL proving a raw literal is refused, so this cannot ship again. `novaclaw:server` is unchanged in count. Nothing moved down.",
  "countsConnectionNameRemoved": "2026-09-22: the model Configure screen's `Connection name (optional)` is GONE, with its machinery. Owner: it served no purpose for the user and cluttered the screen. Removed: the form field and its Identity-tab row, the `defaultProviderName`/`customProviderName` helpers (the only callers), the save's `name:` write (the provider's stored name, set when the provider was added, is left alone), and the three `settings.models.config.providerName.*` i18n keys plus the `SPLIT_DESC` ledger entry. The provider name is a shared config field and stays — only this surface stopped writing it. `app:unit` and `app:browser` are unchanged in count: `dialog-model-config.test.ts`'s 'allows a concise connection name' case is replaced by a shrink-only ratchet that the field, both helpers and all three keys are ABSENT, and the browser case now asserts the first Identity row's `?` is `API path`. Nothing moved down.",
  "countsProbeDetailLayout": "2026-09-22: a long probe detail can no longer collapse the model name. Owner report: the learned-wire note made the Models-list status long and the name rendered one character per line. Cause: the base media query pins the row control at `flex-shrink: 0`, so the COPY absorbed the whole overflow and `overflow-wrap: anywhere` broke the name per character; `align-items: flex-end` on the actions column also sized the status to its own max-content, so it never wrapped. Fix: the control shrinks (`min-width: 0; flex-shrink: 1`) down to the buttons' min-content, the actions stretch, and the result is stretched and bounded; the learned-wire detail is also shorter (`Switched this model's API to …`). `app:unit` 1783->1784 is +1, ADDED to `models-enablement.test.ts`: a ratchet pinning the shrink, the stretch and the result's `align-self`. Nothing moved down.",
  "countsAvatarPool": "2026-09-22: core 7380->7386 adds six avatar guards: all 100 separate WebPs exist, are distinct and are square 250px crops; an agent without an upload receives a stable pool portrait; active agents receive unique assignments and retirement returns its slot; the config-store seam distinguishes retirement from a protected agent's reset; established officers retain their original portraits without consuming pool slots; and Nova always uses the reserved star portrait. Every delta is a test ADDED; nothing moved down.",
  "units": {
    "llm": 28,
    "core": 9,
    "novaclaw:server": 7,
    "novaclaw:v2": 3,
    "schema": 0,
    "protocol": 0,
    "effect-drizzle-sqlite": 0,
    "http-recorder": 0,
    "sdk-js": 0,
    "session-ui": 0,
    "ui": 0,
    "app:unit": 0,
    "app:browser": 0,
    "desktop": 0,
    "server": 0,
    "novaclaw:config": 0,
    "novaclaw:tool": 0,
    "novaclaw test/*": 3
  },
  "counts": {
    "script": 268,
    "script-lib": 5,
    "schema": 61,
    "protocol": 36,
    "effect-drizzle-sqlite": 7,
    "http-recorder": 33,
    "llm": 498,
    "host": 11,
    "sdk-js": 46,
    "session-ui": 231,
    "ui": 67,
    "core": 7386,
    "app:unit": 1784,
    "app:browser": 262,
    "desktop": 239,
    "server": 114,
    "novaclaw:server": 444,
    "novaclaw:v2": 4,
    "novaclaw:config": 143,
    "novaclaw:tool": 43,
    "novaclaw:control-plane": 21,
    "novaclaw:fixture": 31,
    "novaclaw:cli-contract": 2,
    "novaclaw test/*": 771,
    "novaclaw test/cli/lazy-command.test.ts": 3,
    "novaclaw test/cli/run/run-process.test.ts": 17,
    "novaclaw test/mcp/config-reload.test.ts": 4
  },
  "failing": {
    "novaclaw:server": []
  },
  "peaks": {
    "schema": 1236,
    "protocol": 751,
    "http-recorder": 733,
    "llm": 852,
    "sdk-js": 1549,
    "session-ui": 567,
    "ui": 442,
    "core": 10822,
    "app:unit": 1198,
    "app:browser": 692,
    "desktop": 709,
    "server": 1429,
    "novaclaw:server": 2887,
    "novaclaw:v2": 1062,
    "novaclaw:config": 925,
    "novaclaw:tool": 1983,
    "novaclaw:control-plane": 868,
    "novaclaw:fixture": 910
  },
  "peaksDeliberatelyAbsent": ["script", "effect-drizzle-sqlite"],
  "hostCommitWarnPct": {
    "core": 85
  },
  "hostCommitWarnPctNote": "Per-unit host-commit WARNING lines; anything absent uses the default 75% (heavy-guard's admission line, and the product's own Pressure warning). `core` is here because a flat 75 fired on 15 of its 31 recorded runs -- its MEDIAN host commit is 74% and its max is 80% -- while NO other unit has ever reached 75 at all. A warning that fires on half of a unit's healthy runs is one its reader learns to skip. 85 is derived from core's observed max (80) plus margin, measured 2026-08-13 over 348 unit-rows across 17 full gates. ⚠️ It therefore inherits that measurement's expiry date: if core's normal band moves, re-derive this rather than nudging it. The 90% FLOOR is a separate, product-wide line and is NOT per-unit.",
  "workingSets": {
    "app:browser": 367,
    "app:unit": 489,
    "core": 4861,
    "desktop": 190,
    "http-recorder": 182,
    "llm": 307,
    "novaclaw test/agent-removal-wiring.test.ts": 303,
    "novaclaw test/agent/": 522,
    "novaclaw test/auth/": 290,
    "novaclaw test/cli/lazy-command.test.ts": 303,
    "novaclaw test/effect/": 333,
    "novaclaw test/event-manifest.test.ts": 277,
    "novaclaw test/event-v2-bridge-hoist.test.ts": 295,
    "novaclaw test/filesystem/": 594,
    "novaclaw test/format/": 365,
    "novaclaw test/git/": 304,
    "novaclaw test/instance-registry.test.ts": 304,
    "novaclaw test/local-model/": 283,
    "novaclaw test/mcp/": 525,
    "novaclaw test/mcp/config-reload.test.ts": 333,
    "novaclaw test/memory-ingest-graph.test.ts": 299,
    "novaclaw test/patch/": 287,
    "novaclaw test/permission-task.test.ts": 322,
    "novaclaw test/permission/": 290,
    "novaclaw test/project/": 374,
    "novaclaw test/provider/": 276,
    "novaclaw test/question/": 334,
    "novaclaw test/reassignment-wiring.test.ts": 296,
    "novaclaw test/session-worker/": 292,
    "novaclaw test/skill/": 347,
    "novaclaw test/storage/": 334,
    "novaclaw test/util/": 379,
    "novaclaw:config": 887,
    "novaclaw:control-plane": 299,
    "novaclaw:fixture": 349,
    "novaclaw:server": 1769,
    "novaclaw:tool": 1131,
    "novaclaw:v2": 495,
    "protocol": 264,
    "script": 110,
    "sdk-js": 777,
    "server": 368,
    "session-ui": 122,
    "ui": 131
  },
  "workingSetsNote": "Resident WorkingSet64 peak per run unit -- the RESIDENT half of the two-wall ladder in script/lib/memory-plan.ts. Missing units use the explicit UNPROFILED_RESIDENT_MB default; never infer resident demand from commit charge. RE-BASELINED WHOLE on 2026-09-02 from tmp/peak-series.jsonl, because the concurrent runner turned this map from an advisory into a BUDGET: with only `core` measured, every other unit reserved the 1200 MB GUESS -- 3-10x the measured peak for most of them -- and that alone capped the pool at one unit on a loaded box. Filter = peaksNote's three exclusions PLUS the host-pressure term the 2026-09-01 peaksLadderNote ruling asked for: ok, shards 1, 3+ own ticks, regressed != true, carrying foreignMb (post-attribution rows only), carrying hostCommitPct, and hostCommitPct BELOW that unit's hostCommitWarnPct line (85 for core, 75 otherwise). Each entry is then the LARGEST surviving reading; WHOLE_HEADROOM_FACTOR covers overshoot beyond it. THREE units are left ABSENT on fewer than 10 surviving rows and keep the declared guess: effect-drizzle-sqlite (n=2), `novaclaw test/*` (n=9, max 3791) and `novaclaw test/worker-watch-wiring.test.ts` (n=2). WARNING core moved 4616 -> 4861 under this filter and that is not a regression: 4616 was a single 2026-08-09 measurement, and 4861 is the max of 19 quiet runs. The pressure term is what excludes the 7300-8000 MB readings of 2026-08-26..29 -- every one of which is also regressed:true, i.e. the episode the ratchet caught, which peaksNote already says is not a baseline. Evidence: notes/reports/gate-concurrency-2026-09-02.md.",
  "peaksNote": "Measured peak MB per run unit - the input to the memory ladder in script/lib/memory-plan.ts. RE-BASELINED WHOLE on 2026-08-13 from the attributed rows of tmp/peak-series.jsonl, superseding the 2026-08-07 baseline. Each entry is the LARGEST HEALTHY WHOLE-RUN reading for that unit; WHOLE_HEADROOM_FACTOR then covers overshoot beyond that maximum. THREE exclusions, each of which a naive max gets wrong: (1) SHARDED rows, because a shard's window carries the previous shard's unreclaimed memory - that is why `core` read 17,958 while no whole run ever exceeded 10,822, and feeding it back is what kept core permanently sharded; (2) rows with fewer than 3 own-ticks, which are lower bounds; (3) rows flagged `regressed: true`, because a regression the ratchet caught is not a baseline - a plain max would have enshrined novaclaw:server at 9,303 and app:unit at 1,918, the exact rows that went red. `schema` is kept at its 2026-08-07 value on purpose: it is a FLOOR from an under-sampled bound, and the healthy series has only 2 rows. Piecemeal editing is how the dead 1007 MB core entry survived four gates, so re-baseline the set or none of it.",
  "peaksScaleNote": "🔴 ROWS ARE NOT COMPARABLE ACROSS 2026-08-07 app 2d6ad8db7. Before it the sampler summed every `bun` on the box by name and every reading also carried the `bun run test` parent shim; after it a sample is attributed by process BIRTH TIME and carries `foreignMb`. No arithmetic converts an old row into a new one — `schema` read 43 (the shim alone, its own process caught by no tick) and reads 552-1236 now. Only rows carrying `foreignMb` were used here. See todo/test-speed.md.",
  "peaksUnsampledNote": "TWO units have no entry ON PURPOSE and must not be given one from the series: `script` and `effect-drizzle-sqlite`. Absent, they plan with UNPROFILED_PEAK_MB (1200), a declared guess. CORRECTED 2026-08-13: the original reason was 'no attributed run reached 3 own-ticks', and for `script` that is no longer true -- 28 runs now have 3+ ticks, max 345 MB, so the tool started printing 'copy it into peaks'. Do NOT. Across the whole series `script` has been observed at 625 MB, so its well-sampled runs are missing the peak by nearly 2x; 3 samples is enough to stop LOOKING thin, not enough to have SEEN the peak. Writing 345 in would under-reserve against a real observation, which is the same bug in a new disguise. `effect-drizzle-sqlite` still has only 2 well-sampled runs (max 634) against a 688 MB observation. Both are now listed in `peaksDeliberatelyAbsent`, which `script/test.ts` reads, so the printed advice and this note can no longer disagree. `schema` is the exception and is listed in `peaks`: it is equally under-sampled, but its largest bound (1236) EXCEEDS the 1200 guess, so the guess is falsified and the bound is the safer of the two. Its entry is a floor, not a peak.",
  "peaksLadderNote": "Commit and resident demand are planned against independent walls as of 2026-08-09; never collapse them through min(free RAM, commit headroom). CORRECTED 2026-09-01: this note used to say `core` carries a 17958 MB commit profile. It does not, and has not since the 2026-08-13 whole re-baseline three lines up -- 17958 was a SHARDED row, which is exactly what peaksNote excludes. The live pair is peaks.core 10822 commit / workingSets.core 4616 resident. Re-measured 2026-09-01 over healthy whole runs in tmp/peak-series.jsonl (shards 1, not regressed, 3+ own ticks, carrying foreignMb): 44 core runs span 7921-16310 MB commit and 2422-7971 MB resident. SIX of the 44 exceed the committed 10822, and all six are 2026-08-25 runs taken at 74-85% host commit -- so peaks.core may now be an under-reservation on a loaded box and wants a re-baseline, not an edit here. Sharding core still mitigates little because the bulk of its commit belongs to sixteen bun workers spawned by one flock test file. RULING 2026-09-01, on the six: DO NOT raise peaks.core to fit them. The healthiness filter used for that re-measurement (shards 1, not regressed, 3+ own ticks, carrying foreignMb) has NO host-pressure term, so it cannot see the one thing those six share -- they were taken at 74-85% host commit, i.e. inside the band this same file already distrusts via hostCommitWarnPct.core = 85. They are not healthy runs that exceed the ceiling; they are contended runs the filter is blind to. Raising 10822 toward 16310 would bake machine contention into a UNIT's profile and leave the ratchet unable to see a real ~50% regression -- a bound that measures the probe rather than the subject, which is the failure this whole ladder exists to avoid. The fix is to give the healthiness filter a host-pressure term (reuse hostCommitWarnPct) and re-measure over quiet runs only; if the quiet population still exceeds 10822, THAT is a re-baseline and it is earned. Whoever does it: report how many runs survived the pressure filter, because a ceiling re-derived from a handful of samples is the same mistake one level down."
}
