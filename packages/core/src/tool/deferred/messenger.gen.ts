// Generated by packages/core/script/deferred-builtins.ts. Do not edit.
import { ToolDefinition } from "@novaclaw/llm"
import { makeLocationNode } from "../../effect/app-node"
import { LazyBuiltin } from "../lazy-builtin"
import { Location } from "../../location"
import { LocationMutation } from "../../location-mutation"
import { MessengerDrivers } from "../../messenger/drivers"
import { MessengerStore } from "../../messenger/store"
import { PermissionV2 } from "../../permission"
import { SessionStore } from "../../session/store"
import { ToolRegistry } from ".././registry"

export const node = makeLocationNode({
name: "tool/messenger",
layer: LazyBuiltin.layer({
definition: new ToolDefinition({
  "name": "messenger",
  "description": "Read and send the user's real messages AND EMAILS through their connected accounts — chat apps (Telegram, Discord, IRC) AND email mailboxes (Gmail, Outlook, any IMAP account). THIS TOOL IS your access to the user's email and messaging. Whenever the user mentions email, mail, their inbox, Gmail/Outlook, a chat, or 'my messages', do NOT assume you have no access — START by calling {\"op\":\"status\"} to see which accounts are actually connected. Ops: status (connected accounts + connection state + this session's bindings) · chats (list an account's conversations / recent EMAIL THREADS — subjects + senders; ids feed the other ops) · history (recent messages / emails of one chat or thread, oldest first — use it to read and summarize a mailbox or conversation) · send (write into a chat / reply to an email thread AS the user, paced at human speed; writing to a chat that has never messaged this account is a COLD START — refused unless you pass initiate:true, which asks the user for permission and is strictly capped per day, so prefer asking people to message first) · connect (bind THIS session to a chat/thread — pick a trust tier) · disconnect · upload (send a workspace file, optional caption) · download (save every attachment on one message) · moderate (delete a message, or ban/kick/mute/pin a member — for chats you moderate, where the platform supports it). To summarize a mailbox: {\"op\":\"status\"} → {\"op\":\"chats\",\"account\":\"<id or label>\"} (recent threads) → optionally {\"op\":\"history\",\"chat\":\"<id>\"} for bodies → summarize. The user's messages and emails are private: handle them inside this workspace and never forward them anywhere without being asked. ALSO USE THIS TOOL AS A RESEARCH SOURCE — it reads platforms the open web cannot. Many sources publish ONLY inside a chat platform: Telegram channels, Discord announcement/news channels (indie studios often post releases there before anywhere else), subreddits. Their web pages are JavaScript-only or blocked, so webfetch returns an empty shell. When research leads to one of those and the account is connected, read it HERE: {\"op\":\"chats\",\"account\":\"<id>\"} to find the channel, then {\"op\":\"history\",\"chat\":\"<id>\"} for the posts. This is the SANCTIONED route — the user's own account reading a public channel — not scraping, so prefer it over trying to fetch the platform's website. Cite the channel and post date like any other source. ⚠️ For RESEARCH, pass purpose:\"research\" on `history` and read ONLY chats `chats` labels \"public source\". The label is per chat and the user owns it, so the chat KIND tells you nothing about it: a server text channel can be a public news feed or a company's private staff room. \"private — never cite\" is the user's correspondence and \"unlabelled\"/\"unconfirmed\" means nobody has said yet — in all three cases you may still read the chat as correspondence when the user asked you to, but nothing from it may be quoted, summarized or cited outside this conversation. If you need one of those as a source, ask the user to mark it public in Settings → Messengers; you cannot mark it yourself, and that is deliberate.",
  "inputSchema": {
    "anyOf": [
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "status"
            ]
          }
        },
        "required": [
          "op"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "chats"
            ]
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or its label — omit when only one account exists"
          }
        },
        "required": [
          "op"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "history"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id (from `chats`) whose recent messages to fetch"
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          },
          "limit": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Max messages (default 50, cap 200)"
          },
          "purpose": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "correspondence",
                  "research"
                ]
              },
              {
                "type": "null"
              }
            ],
            "description": "What this read is FOR. 'correspondence' (the default) = handling the user's own messages, and stays inside this conversation. 'research' = the content may be quoted or cited in something that leaves this chat — allowed ONLY for chats the user has marked as a public source, and refused with a reason otherwise. Say 'research' whenever you intend to cite it."
          }
        },
        "required": [
          "op",
          "chat"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "send"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id (from `chats`) to write into"
          },
          "text": {
            "type": "string",
            "description": "The message text — sent AS the user, paced at human typing speed"
          },
          "reply": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Message id this answers (from the `msg …` in a message header, or from history) — attaches the reply to that message. Use it in busy group chats so people can tell what you're answering."
          },
          "initiate": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set true ONLY to START a conversation with someone who has never written to this account (a cold first contact). It asks the USER for permission every time, and only a small number of new conversations are allowed per day. Leave it off for every reply and for every chat that has already messaged you — and prefer asking the person to message first, which needs no permission at all. It cannot be used from a chat belonging to a client or an audience."
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          }
        },
        "required": [
          "op",
          "chat",
          "text"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "connect"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id (from `chats`) to bind THIS session to — inbound messages become your turns"
          },
          "trust": {
            "type": "string",
            "enum": [
              "operator",
              "client",
              "audience"
            ],
            "description": "Who is on the other side — operator (you/family, full control), client (a customer whose requests you treat carefully), or audience (the public you only moderate). REQUIRED — ask the user if unsure."
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          },
          "force": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set true ONLY after confirming with the user, to bind an untrusted client/audience chat to a session that auto-approves everything (bypass/yolo permission mode)."
          }
        },
        "required": [
          "op",
          "chat",
          "trust"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "disconnect"
            ]
          },
          "chat": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Chat id to unbind (default: this session's binding)"
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id or label — omit when only one account exists"
          }
        },
        "required": [
          "op"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "upload"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id (from `chats`) to send the file into"
          },
          "path": {
            "type": "string",
            "description": "Workspace file to send (relative to this session's folder)"
          },
          "caption": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Short text sent with the file"
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          }
        },
        "required": [
          "op",
          "chat",
          "path"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "download"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id the message with the attachment is in"
          },
          "message": {
            "type": "string",
            "description": "Message id carrying the attachment(s) (shown in message headers and history)"
          },
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Where to save, relative to this session's folder — a file name for a single attachment, a folder when the message carries several (default: downloads/)"
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          }
        },
        "required": [
          "op",
          "chat",
          "message"
        ],
        "additionalProperties": false
      },
      {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "moderate"
            ]
          },
          "chat": {
            "type": "string",
            "description": "Chat id (from `chats`) the moderation acts in"
          },
          "act": {
            "type": "string",
            "enum": [
              "delete",
              "ban",
              "kick",
              "mute",
              "pin",
              "approve",
              "lock"
            ],
            "description": "delete a message · ban/kick/mute a member (needs a SERVER chat) · pin a message · approve a removed or reported item back into the listings · lock this chat against new replies. Not every platform has every act — a miss comes back saying so."
          },
          "message": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Message id — required for delete, pin and approve"
          },
          "user": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "User id — required for ban, kick, and mute"
          },
          "seconds": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "For mute: how long the timeout lasts (default 600, platform-capped). For ban: ALSO delete that member's messages from the last N seconds, where the platform can (a spam wave) — not every platform can, and one that can't says so."
          },
          "days": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Ban only: make it temporary, this many days. Platforms without temporary bans refuse rather than ban forever."
          },
          "account": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Account id (msa_…) or label — omit when only one account exists"
          }
        },
        "required": [
          "op",
          "chat",
          "act"
        ],
        "additionalProperties": false
      }
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "outcome": {
        "type": "string",
        "enum": [
          "ok",
          "failed",
          "unavailable"
        ]
      },
      "message": {
        "type": "string"
      }
    },
    "required": [
      "outcome",
      "message"
    ],
    "additionalProperties": false
  }
}),
sideEffect: "non-idempotent",
load: () => import("../messenger"),

}),
deps: [
    ToolRegistry.node,
    MessengerStore.node,
    MessengerDrivers.node,
    PermissionV2.node,
    Location.node,
    LocationMutation.node,
    SessionStore.node,
  ],
})
